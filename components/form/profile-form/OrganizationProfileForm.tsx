import { StyleSheet } from 'react-native';
import { BaseProps } from '@/constants/Types';

type Props = BaseProps & {

};

const OrganizationProfileForm = ({}: Props) => {
  return (<></>);
};

const styles = StyleSheet.create({
  container: {
    
  },
});

export default OrganizationProfileForm;