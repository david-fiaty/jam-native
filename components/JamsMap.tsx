import { StyleSheet, View, Text } from 'react-native';

const JamsMap = () => {
  return (
    <View>
      <Text>Jams map</Text>
    </View>
  );
};

const styles = StyleSheet.create({

});

export default JamsMap;