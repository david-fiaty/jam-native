import { StyleSheet, View, Text } from 'react-native';

const AddJam = () => {
  return (
    <View>
      <Text>Add jam</Text>
    </View>
  );
};

const styles = StyleSheet.create({

});

export default AddJam;