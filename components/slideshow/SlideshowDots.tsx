import { StyleSheet, TouchableOpacity, View } from "react-native";

type Props = {
  index: number;
  count?: number;
};

const SlideshowDots = ({ index, count }: Props) => {

  return (<></>);
};

const styles = StyleSheet.create({

});

export default SlideshowDots;
