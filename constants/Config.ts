export const Config = {
  cacheEnabled: true,
  apiEnabled: false,
  apiUrl: 'https://dev.jammm.app/backend/api',
  defaultLanguage: 'en',
  fallbackLanguage: 'en',
  appNamespace: 'jam-native',
  logoImageSize: 512,
};
