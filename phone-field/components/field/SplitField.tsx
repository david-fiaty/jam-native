import { Text } from "react-native";
import { SplitFieldProps } from "@/phone-field/types/field";

const SplitField = ( props: SplitFieldProps ) => {
  return (
    <Text>Split phone widget</Text>
  );
};

export default SplitField;